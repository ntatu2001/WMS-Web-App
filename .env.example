# Base URL của WarehouseAPI backend, dùng chung cho axiosClient và SignalR hub.
# Copy file này thành .env (hoặc .env.local) và chỉnh lại theo môi trường của bạn.
VITE_API_BASE_URL=http://localhost:5037/WarehouseAPI/
